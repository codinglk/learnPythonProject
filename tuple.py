# tuple is immutable, for tuples we need to use () and for list we need to use []
coordinates = (4, 5)
print(coordinates[0])
print(coordinates[1])

# list of tuples
coordinates1 = [(4, 5), (7, 9), (2, 4)]
print(coordinates1[0])
print(coordinates1[1])
